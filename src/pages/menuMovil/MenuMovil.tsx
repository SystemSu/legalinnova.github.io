import { Drawer, List, ListItem } from "@mui/material";
import { useState } from "react";

interface IMenu {
  valor: boolean;
}

export const MenuMovil: React.FC<IMenu> = ({ valor }) => {


  return (
    <div>
        <ul>
            <li>
                s
            </li>
            <li>
                ss
            </li>
        </ul>
    </div>
  );
};
